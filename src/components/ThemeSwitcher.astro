---
// Theme Switcher Component - No props needed
---

<div class="theme-switcher">
  <button class="theme-button" data-theme="light" title="Light Theme">
    <span class="theme-icon">☀️</span>
  </button>
  <button class="theme-button" data-theme="dark" title="Dark Theme">
    <span class="theme-icon">🌙</span>
  </button>
  <button class="theme-button" data-theme="brown" title="Brown Theme">
    <span class="theme-icon">🟤</span>
  </button>
  <button class="theme-button" data-theme="cool" title="Cool Theme">
    <span class="theme-icon">❄️</span>
  </button>
  <button class="theme-button" data-theme="summer" title="Summer Theme">
    <span class="theme-icon">🌴</span>
  </button>
</div>

<style>
  .theme-switcher {
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }

  .theme-button {
    width: 40px;
    height: 40px;
    border: 1px solid var(--color-border);
    border-radius: 8px;
    background: var(--color-surface);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
  }

  .theme-button:hover {
    border-color: var(--color-primary);
    transform: scale(1.1);
  }

  .theme-button.active {
    border-color: var(--color-accent);
    box-shadow: 0 0 0 2px var(--color-accent);
  }

  .theme-icon {
    font-size: 1.25rem;
  }
</style>

<script>
  // Theme definitions
  const themes = {
    light: {
      primary: '#5479f7',
      accent: '#ff0077',
      background: '#ffffff',
      surface: '#f3f4f6',
      text: '#1f2937',
      textSecondary: '#4b5563',
      textTertiary: '#9ca3af',
      border: '#e5e7eb',
    },
    dark: {
      primary: '#5479f7',
      accent: '#ff0077',
      background: '#0f172a',
      surface: '#1e293b',
      text: '#f1f5f9',
      textSecondary: '#cbd5e1',
      textTertiary: '#64748b',
      border: '#334155',
    },
    brown: {
      primary: '#8b6914',
      accent: '#c4a35a',
      background: '#faf6ed',
      surface: '#f5edd9',
      text: '#3d2e1f',
      textSecondary: '#6b5d4f',
      textTertiary: '#9a8c7e',
      border: '#d4c9b8',
    },
    cool: {
      primary: '#0891b2',
      accent: '#06b6d4',
      background: '#f0fdfa',
      surface: '#ccfbf1',
      text: '#134e4a',
      textSecondary: '#155e59',
      textTertiary: '#0f766e',
      border: '#99f6e4',
    },
    summer: {
      primary: '#f59e0b',
      accent: '#f97316',
      background: '#fffbeb',
      surface: '#fef3c7',
      text: '#78350f',
      textSecondary: '#92400e',
      textTertiary: '#b45309',
      border: '#fde68a',
    },
  };

  // Get or set theme
  function getTheme() {
    return localStorage.getItem('theme') || 'brown';
  }

  function setTheme(themeName) {
    const theme = themes[themeName];
    if (!theme) return;

    const root = document.documentElement;
    root.style.setProperty('--color-primary', theme.primary);
    root.style.setProperty('--color-accent', theme.accent);
    root.style.setProperty('--color-background', theme.background);
    root.style.setProperty('--color-surface', theme.surface);
    root.style.setProperty('--color-text', theme.text);
    root.style.setProperty('--color-text-secondary', theme.textSecondary);
    root.style.setProperty('--color-text-tertiary', theme.textTertiary);
    root.style.setProperty('--color-border', theme.border);

    localStorage.setItem('theme', themeName);

    // Update active button
    document.querySelectorAll('.theme-button').forEach(btn => {
      btn.classList.remove('active');
      if (btn.dataset.theme === themeName) {
        btn.classList.add('active');
      }
    });
  }

  // Initialize theme on load
  const currentTheme = getTheme();
  setTheme(currentTheme);

  // Add event listeners to buttons
  document.querySelectorAll('.theme-button').forEach(button => {
    button.addEventListener('click', () => {
      const theme = button.dataset.theme;
      setTheme(theme);
    });
  });
</script>
